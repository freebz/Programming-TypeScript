function map(array, f) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        result[i] = f(array[i]);
    }
    return result;
}
function map(array, f) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        result[i] = f(array[i]);
    }
    return result;
}
